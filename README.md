# SID DDEPIA-Menoua MVP

Ce dépôt contient le code source du MVP du Système d'Information Décisionnel de la Délégation Départementale de l'Élevage, des Pêches et des Industries Animales de la Menoua (DDEPIA-Menoua).

## Pré-requis

Pour exécuter ce projet localement, vous devez avoir installé :
- **Node.js** (version 18 ou supérieure recommandée) : [Télécharger Node.js](https://nodejs.org/)
- **PostgreSQL** (version 14 ou supérieure avec l'extension PostGIS) : [Télécharger PostgreSQL](https://www.postgresql.org/download/)

## Installation

1. **Cloner ou ouvrir le projet** dans votre terminal.
2. **Installer les dépendances** :
   ```bash
   npm install
   ```
3. **Configurer les variables d'environnement** :
   - Copiez le fichier `.env.example` en `.env`
   - Modifiez `DATABASE_URL` avec vos identifiants PostgreSQL.
4. **Initialiser la base de données** :
   ```bash
   npx prisma migrate dev --name init
   npx prisma db seed
   ```
5. **Lancer le serveur de développement** :
   ```bash
   npm run dev
   ```
6. **Accéder à l'application** : Ouvrez `http://localhost:3000` dans votre navigateur.

## Déploiement (VPS Ubuntu)

Pour déployer sur un VPS en production :
1. Installer Node.js, PostgreSQL et Nginx sur le VPS.
2. Cloner le projet.
3. Créer la base de données et appliquer les migrations (`npx prisma migrate deploy`).
4. Compiler le projet : `npm run build`.
5. Lancer avec PM2 : `pm2 start npm --name "sid-menoua" -- start`.
6. Lancer le planificateur de relances dans un process séparé : `pm2 start npm --name "sid-menoua-cron" -- run cron`.
7. Configurer Nginx comme reverse proxy vers le port 3000.

## État actuel

- ✅ Seed généralisé aux 28 tableaux (402 champs) à partir du dictionnaire de données validé (`prisma/data/DICO_DONNEES_CANEVAS_MENSUEL_MENOUA.xlsx`, parsé par `prisma/seed-lib/parseDictionnaire.ts`).
- ✅ Authentification bcrypt, contrôle des droits serveur (`lib/permissions.ts`) + middleware par rôle.
- ✅ Saisie offline générique (FormMatrice/FormNominatif/FormEvenement) pilotée à 100 % par la base, aucun tableau codé en dur.
- ✅ Synchronisation idempotente, verrouillage à la soumission, déverrouillage exceptionnel audité.
- ✅ Contrôle sectoriel (vue croisée + correction tracée + validation de section), synthèses d'analyse, supervision DD.
  - **Validation BAC non bloquante pour le rapport mensuel** (`verifierCompletudeDD` dans `src/server/export/rapport-docx.ts`) : le BAC ne contrôle aucun des 28 tableaux (colonne "Section resp." du dictionnaire) — seule sa synthèse qualitative existe, pas de chiffres à valider. Demande explicite du DD : sa validation n'empêche donc plus la génération du rapport DD **pour les périodes MENSUEL** ; SSV et PSA et SPAIH restent bloquants. Cette exemption redeviendra caduque quand les périodes trimestrielle/semestrielle/annuelle seront implémentées (phase ultérieure) — le DD a précisé que la validation du BAC y sera alors impérative.
- ✅ Export DREPIA (.xlsx) versionné et haché.
- ✅ **Génération des rapports .docx (DA et DD)**, `templates/rapport_mensuel_DA.docx` et `rapport_mensuel_DD.docx` générés depuis `prisma/seed-lib/buildReportTemplates.ts` (structure fidèle à `CANEVAS STAT MENOUA.doc` fourni par le DD : en-tête bilingue République/Republic, 5 sections, 28 tableaux, totaux, synthèses de sections). Moteur d'agrégation dans `src/server/export/rapport-docx.ts`, route `POST /api/reports/generate` (`{periodeId, type:"DD"|"DA"}`). Vérifié de bout en bout : valeurs saisies → corrigées → retrouvées exactes dans le .docx généré, workflow incomplet correctement bloqué (409 avec diagnostic).
  - **Mise en page fidèle au canevas, tableau par tableau** (exigence explicite du DD — le rapport doit être organisé exactement comme le document papier officiel, pas selon une présentation uniforme) : `prisma/seed-lib/canevasLayout.ts` décrit, pour chacun des 28 tableaux, sa mise en page réelle — source unique partagée par les trois générateurs (DD, DA, exact) et le moteur d'agrégation. Lignes = arrondissements pour 1.1/1.2/2.1/2.2/2.5/2.6 (colonnes = espèces/produits) ; tableau départemental unique sans colonne par arrondissement pour 2.4/3.4/5.1–5.6 (conforme au canevas, qui ne ventile pas ces tableaux par arrondissement) ; liste dynamique d'établissements (boucle docxtemplater sur les lignes de tableau) pour les tableaux NOMINATIF (1.3, 1.4, 1.5, 2.3) ; liste dynamique des événements déclarés (avec résolution des codes référentiels en libellés) pour les tableaux EVENEMENT (3.1–3.5, 4.1–4.4). Le rapport DD ajoute une colonne « Arrondissement » aux listes NOMINATIF/EVENEMENT (consolidation de 6 arrondissements) ; le rapport DA (fiche de collecte d'un seul arrondissement) reprend les **mêmes colonnes que le canevas** (espèces/produits en colonnes, comme le DD) mais avec une seule ligne de données + une ligne « Mois Précédent », et sans la colonne Arrondissement sur les listes NOMINATIF/EVENEMENT — corrigé après qu'un premier passage a laissé le rapport DA sur l'ancien format générique (lignes = indicateurs) alors que le DD et le document "exact" avaient déjà été repris. Ligne « TOTAL MOIS COURANT »/« TOTAL MOIS PRÉCÉDENT » ajoutée au bas de tous les tableaux NOMINATIF et EVENEMENT (DD, DA, exact), conformément au canevas — absente du premier passage.
  - **Quatre derniers écarts corrigés** (audit demandé par le DD, comparaison directe canevas ↔ .docx généré) : (1) graphie des arrondissements alignée sur le canevas littéral (« DSCHANG », « FONGO TONGO », sans accent ni tiret) au lieu de la graphie accentuée de la base ; (2) en-têtes à 2 niveaux (tableau 2.6 : Viande/Lait/Peaux/Cuirs/Poisson fumé) et 3 niveaux (tableaux 5.1–5.6 : Nombre Mois Courant → Mis en vente/Vendus → Décades, avec Catégories/Prix moyen fusionnés verticalement) reconstruits avec de vraies cellules fusionnées (`columnSpan`/`verticalMerge` du package `docx`), plus qu'un en-tête à plat ; (3) colonne « Lieux » du tableau 2.1 (absente du dictionnaire) : nouveau champ `T21_LIEUX` de type TEXTE ajouté au dictionnaire xlsx, à `SaisieMatrice.valeurTexte` (migration), saisie via un champ texte dans `FormMatrice.tsx`, synchronisé et affiché comme les autres colonnes (jamais sommé) ; (4) tableau 1.6 : le canevas a bien DEUX colonnes distinctes « Pêche maritime artisanale et semi-Industrielle » (confirmé sans ambiguïté sur le XML du .doc source — aucune fusion de cellule) alors que le dictionnaire ne porte qu'un champ par espèce pour la pêche maritime ; faute de source de données distincte, la même valeur est dupliquée dans les deux colonnes pour rester fidèle à la structure — **à confirmer avec le DD** si une vraie ventilation artisanale/semi-industrielle existe et doit être saisie séparément.
  - **Lisibilité des tableaux 3.1-3.5/4.1-4.4 et unités du tableau 3.4** (demande explicite du chef de section vétérinaire, qui doit vérifier minutieusement chaque arrondissement contre le terrain) : deux défauts réels trouvés en inspectant le contenu exact d'un rapport DD généré. (1) Tableau 3.4 (inspection des denrées) : l'unité de chaque produit était concaténée dans le libellé (`Beurre (BOITE (1KG))`) au lieu d'une colonne « Unité de Mesure » dédiée comme au canevas, et l'ordre des colonnes était inversé (mesure d'abord, mois ensuite, au lieu de mois d'abord) — `renderDeptTablePrec`/`renderDeptTablePrecDA` reconstruits avec un en-tête fusionné à 2 niveaux (Produit/Unité fusionnés verticalement, Mois Courant/Mois Précédent fusionnés horizontalement), `unite` porté séparément dans `canevasLayout.ts`. (2) Tableaux 3.1-3.5/4.1-4.4 (rapport DD uniquement — la fiche DA reste inchangée, un seul arrondissement) : listes à plat de plusieurs centaines de lignes avec le nom de l'arrondissement répété sur chaque ligne, sans séparation visuelle ni sous-total intermédiaire. `renderEvenementLoopGroupe` (nouveau) regroupe désormais chaque arrondissement sous un en-tête fusionné/ombré (boucle docxtemplater imbriquée `{#T31}{ARR_NOM}...{#items}...{/items}...{/T31}`, données groupées par `evenementLoopRowsGroupes` dans `rapport-docx.ts`) avec un sous-total par arrondissement en plus du total départemental final, inchangé. La fiche de collecte « exact canevas » (`rapport_mensuel_exact.docx`) reste volontairement à plat pour ces tableaux (le canevas papier littéral n'a pas de colonne Arrondissement, sauf 3.3/DAEPIA) — `genererPayloadDD(periodeId, agregerEvenementsParArrondissement)` accepte un second paramètre pour ne pas régresser ce document. Vérifié en générant les trois documents (DD, DA, exact) avec de vraies données et en inspectant le texte exact du .docx produit (pas seulement le code) : structure conforme au canevas pour 3.4, regroupement + sous-totaux corrects pour le DD, aucune régression sur le DA ni sur l'exact.
  - **Tableau 5.6 aligné sur le canevas officiel** (et non plus sur la simplification du dictionnaire) : 4 catégories équines (Âne, Étalon, Jument, Poulain — 28 champs), conformément à la demande du DD ("le canevas est celui que la région connaît et utilise"). Les 14 anciens champs `T56_ASIN_EQUIN_*` sont désactivés et tracés (jamais supprimés), remplacés par `T56_{ANE|ETALON|JUMENT|POULAIN}_*`. Le dictionnaire xlsx source a été corrigé en conséquence.
  - **Troisième document : fiche de collecte "exact canevas"** (`templates/rapport_mensuel_exact.docx`, généré par `buildDocExact` dans `buildReportTemplates.ts`, route `POST /api/reports/generate` avec `type:"EXACT"`, réservé au DD, bouton « Générer la fiche de collecte (.docx) » sur `/dd/supervision`). Reproduit la transcription exacte du canevas fournie par le DD, sans les enrichissements du rapport DD : mêmes totaux de colonnes ajoutés au canevas (2.4, 5.1–5.6, 1.6, 1.7 — calculés en sommant les catégories du tableau, `T{code}_COLTOTAL_{i}` / `T16_ROWTOTAL_*` / `T17_ROWTOTAL_*`), mais aucune colonne « Arrondissement » sur les tableaux NOMINATIF/EVENEMENT (sauf 3.3, qui regroupe déjà par DAEPIA dans le canevas) et pas de synthèses d'analyse. Inclut le tableau « Tableau XVI : Transit », doublon du 4.3 avec colonnes Arrondissement + Observations en plus, présent tel quel dans le document officiel. Réutilise l'agrégation départementale de `genererPayloadDD` (nouvelle enum `TypeExport.RAPPORT_EXACT_DOCX`, migration `add_rapport_exact_docx_type`).
- ✅ **Sauvegarde locale manuelle du brouillon DA** (`src/components/BackupLocalButton.tsx`, sur `/da/saisie`) : en plus de l'enregistrement automatique dans le navigateur, le DA peut télécharger un fichier `.json` de ses saisies non-synchronisées et le recharger — utile en cas de changement d'appareil ou de perte du stockage local du navigateur. La restauration est strictement filtrée sur le compte connecté (jamais les saisies d'un autre compte, même principe que le cloisonnement Dexie par utilisateur). Vérifié de bout en bout dans le navigateur : export (nombre exact de saisies), puis restauration d'un fichier mixte contenant les saisies de deux comptes différents — seules celles du compte connecté sont acceptées.
- ✅ **Relances calendaires (M9)** : `src/server/cron/triggers.ts` (4 déclencheurs : rappel J-1, verrouillage + retard DA, retard sections, rappel clôture DD) + `src/server/cron/alerts.ts` (planification `node-cron`, process séparé via `npm run cron`) + dispatcher IN_APP/SMS/WHATSAPP (`src/server/notifications/dispatcher.ts`, SMS/WhatsApp journalisés en attente de la passerelle réelle phase 3). Notifications IN_APP visibles sur le tableau de bord de chaque rôle (`GET /api/notifications`). Vérifié en conditions réelles : un bug a été corrigé au passage — une section jamais touchée par son chef n'avait pas de ligne `ValidationSection` et échappait à l'alerte de retard (corrigé dans `seedPeriodeOuverte` + `alerteRetardSections`).
- ✅ **Refonte visuelle complète** selon la maquette Claude Design fournie (`SID DDEPIA-Menoua.dc.html`) : palette et typographie extraites en thème Tailwind (`tailwind.config.js`), `AppShell.tsx` (bandeau supérieur + sidebar de navigation par rôle) remplace l'ancien `AppHeader`, page de connexion et tableau de bord (cartes statistiques + actions rapides) alignés sur la maquette. Écart assumé signalé à l'utilisateur : la maquette met un bouton « Soumettre » par écran de saisie, incompatible avec notre modèle (un seul rapport mensuel soumis globalement) — non repris, la vraie logique de soumission (page d'index) a été conservée.
  - **Bug de sécurité des données corrigé au passage** : IndexedDB (Dexie) est partagé par navigateur, pas par session. Si deux comptes DA se connectaient sur le même appareil, les brouillons non synchronisés du premier apparaissaient (et pouvaient être synchronisés par erreur) sous le second. Corrigé en cloisonnant tout le stockage local par `username` (`src/lib/dexie.ts` v3, migration qui purge les anciens brouillons non attribuables). Reproduit et vérifié : une valeur test envoyée sous un second compte s'est bien retrouvée attribuée à son propre arrondissement en base, sans toucher aux données du premier compte.
- ✅ **Délégation de la saisie à un subalterne (rôle `AGENT_SAISIE`)** : un DA confie parfois la saisie mensuelle à un agent, puis relit avant d'envoyer. Nouveau rôle rattaché au même arrondissement que son DA (créé/autorisé depuis `/admin/utilisateurs` comme un compte DA, identifiant généré `agent.<arrondissement>`) : accès à `/da/saisie` et à la synchronisation, mais **jamais** à `POST /api/rapports/submit` (contrôle serveur, pas seulement caché côté UI — vérifié par appel direct de l'API : 403 même en contournant l'interface). **Traçabilité demandée explicitement par le DD** ("mieux qu'il sache qui a tapé, pour suivre et poser des questions") : `saisiParId` ajouté à `SaisieMatrice`/`SaisieNominative`/`SaisieEvenement` (migration), l'auteur réel (dernier à avoir synchronisé) est affiché en clair dans une colonne « Saisi par » sur les trois familles de formulaires (`FormMatrice`, `FormNominatif`, `FormEvenement`). Vérifié de bout en bout dans le navigateur : compte agent créé par le DD → première connexion → saisie → synchronisation → tentative de soumission bloquée (403) → le DA de l'arrondissement rouvre le même tableau et voit la valeur avec l'attribution exacte de l'agent.
- ✅ **Registre des établissements NOMINATIF (CDC §B.5.3)** : page `/etablissements` (DA : limité à son arrondissement ; DD : sélectionne n'importe quel arrondissement), routes `GET/POST /api/etablissements` et `PATCH /api/etablissements/[id]`. Permet d'ajouter un nouvel établissement (couvoir, ferme de ponte, ferme de poulets de chair, provenderie) et de corriger un nom/localité/propriétaire/téléphone déjà existant — les données de démonstration du seed ne sont donc plus figées. Jamais de suppression définitive : un établissement fermé est désactivé (`actif=false`), il disparaît des listes de saisie (1.3/1.4/1.5/2.3) mais reste dans l'historique. Chaque création/modification est tracée dans `AuditLog`. Vérifié de bout en bout dans le navigateur : ajout, renommage, désactivation, et bascule d'arrondissement côté DD — chaque changement retrouvé exact en base.
- ✅ **Module d'administration des comptes (M1, CDC §4.1)**, absent jusqu'ici (seuls les comptes de test du seed existaient). Page DD `/admin/utilisateurs` : création d'un compte (nom, rôle, arrondissement ou section, identifiant généré automatiquement avec suffixe anti-collision, mot de passe temporaire affiché une seule fois), autorisation/désactivation, réinitialisation de mot de passe. Un compte créé démarre **inactif** : la personne concernée ne peut se connecter qu'après autorisation explicite du DD dans l'appli (réutilise `actif`/`lastLoginAt` existants, aucune migration de schéma). À sa première connexion, elle est automatiquement redirigée vers `/mon-compte/premiere-connexion` pour confirmer son nom, renseigner téléphone/WhatsApp/e-mail et choisir son mot de passe définitif (8 caractères min.) — `mustChangePassword` bascule alors à `false` et la session est rafraîchie sans reconnexion (callback NextAuth `trigger:"update"`). Testé de bout en bout dans le navigateur : création → autorisation → connexion au mot de passe temporaire → complétion du profil → reconnexion au nouveau mot de passe, données bien persistées en base à chaque étape. Réservé strictement au DD (`ADMIN_TECH` exclu, conformément à CDC §A.2 : aucun droit métier).

- ✅ **Rapports thématiques « à la carte » (DD)**, fonction additionnelle demandée par le DD pour extraire des données transversales (ex. « tout ce qui concerne les porcins : production, sanitaire, commerce ») sans être limité à la structure fixe du canevas — **le bouton de génération du rapport mensuel classique reste intact et inchangé à tous les niveaux** (DA/DD/EXACT), cette fonction est strictement en plus. Page `/dd/rapports-thematiques` (DD uniquement) : le DD choisit espèce/animal, domaine (sections 1-5 du canevas), arrondissement(s) et période(s), puis génère l'extrait en `.xlsx`, `.docx` ou `.pdf` (nouvelle dépendance `pdfkit`, pur Node, aucun binaire externe requis). Contrairement au rapport mensuel (qui consolide/agrège), ce moteur restitue les **valeurs brutes** ligne par ligne (une ligne par arrondissement×période pour un tableau MATRICE, par établissement×période pour NOMINATIF, par déclaration pour ÉVÉNEMENT) — pensé pour l'analyse, pas la synthèse. L'étiquetage « quel tableau/quelle colonne correspond à quelle espèce » (`src/lib/themeMapping.ts`) est **explicite, pas inféré** : la convention de nommage du dictionnaire n'est pas uniforme (ex. `T21_ABAT_PORCIN` vs `T22_NBABAT_PORC` — même espèce, suffixe différent), une correspondance automatique par texte aurait donné des résultats faux ; chaque tableau a donc été vérifié un par un contre `canevasLayout.ts`. Pas de persistance versionnée façon `ExportDocument` (le modèle suppose une seule période, incompatible avec une extraction multi-mois) — seule une entrée `AuditLog` (`EXPORT_THEMATIQUE`) trace qui a extrait quoi. Vérifié de bout en bout : filtre « Porcins », toutes périodes/domaines confondus → 10 tableaux corrects retenus (1.1, 2.1, 2.2, 2.3, 3.2, 3.3, 3.4, 4.1, 4.3, 5.4), contenu des 3 formats inspecté directement (valeurs réelles, colonnes correctement filtrées à la seule espèce porcine), tableaux sans dimension espèce (2.4, 2.6, 3.1, 3.5...) correctement exclus dès qu'un filtre espèce est actif.
  - **Bug PDF réel trouvé et corrigé après un premier passage** : `pdfkit` échouait systématiquement (500, `ENOENT ... Helvetica.afm`) une fois passé par le vrai bouton — le bundler webpack de Next.js déplace le code compilé dans `.next/server/` sans copier les fichiers de police que `pdfkit` lit sur disque à des chemins relatifs à son propre dossier `node_modules`. Un script de vérification autonome (hors Next.js) avait masqué le problème en le testant sans passer par le bundler. Corrigé en excluant `pdfkit` du bundling (`experimental.serverComponentsExternalPackages` dans `next.config.mjs`) — Next.js fait alors un `require()` normal au runtime, qui résout correctement depuis `node_modules/pdfkit`. Revérifié en cliquant le vrai bouton (pas un script) : les 3 formats fonctionnent, y compris le cas le plus lourd (28 tableaux, aucun filtre).

- ✅ **Espace de maintenance technique (`ADMIN_TECH`)**, jusqu'ici une coquille vide (aucun outil, juste un tableau de bord nu). Quatre écrans sous `/technique/*` (middleware + contrôle serveur strictement réservés à `ADMIN_TECH`, jamais de données métier exposées) :
  - **Santé du système** (`/technique`) : connectivité et latence de la base, compteurs globaux (comptes, arrondissements, rapports, saisies), date/taille de la dernière sauvegarde.
  - **Journal d'audit** (`/technique/audit`) : lecture seule de `AuditLog`, paginé et filtrable par type d'action.
  - **Listes de référence** (`/technique/referentiels`) : gestion de `ReferentielItem` (maladies, vaccins, espèces, actes vétérinaires, motifs de saisie, unités, types d'établissement) — ajout, renommage, désactivation (jamais de suppression, code stable préservé).
  - **Sauvegarde de la base** (`/technique/sauvegarde`) : déclenche un `pg_dump` réel (`storage/backups/*.sql`), liste et téléchargement des sauvegardes existantes.
  - Testé de bout en bout dans le navigateur, chaque outil. **Deux bugs réels trouvés et corrigés pendant le test** : (1) `pg_dump` rejetait le paramètre `?schema=public` de `DATABASE_URL` (spécifique à Prisma, pas du libpq standard) — corrigé en l'extrayant pour le passer en option `-n` séparée ; (2) l'expression régulière de validation du nom de fichier de téléchargement n'autorisait pas le `Z` final de l'horodatage ISO, bloquant tout téléchargement — corrigée.
  - **Circuit de validation DD pour les catégories « structurelles »** (`ESPECE`, `VOLAILLE`, `ESPECE_HALIEUTIQUE`) : demande explicite du DD — l'admin technique doit pouvoir faire évoluer les listes de référence dans le temps (les figer en dur n'est pas réaliste), mais tout ajout qui créerait une **nouvelle colonne dans le rapport officiel** (tableaux 1.1/1.2/1.7) doit d'abord être validé par le DD, jamais pris en compte automatiquement. Les autres catégories (maladies, vaccins, actes vétérinaires, motifs de saisie, unités, types d'établissement) restent effectives immédiatement — elles n'affectent que des menus déroulants de saisie, pas la structure du rapport. Un ajout dans une catégorie structurelle est créé avec `enAttenteValidationDD=true` (badge « En attente DD » visible sur `/technique/referentiels`) ; le DD les traite sur `/dd/referentiels` (« Valider » ou « Rejeter »). À la validation, le ou les `FormField` correspondants sont créés automatiquement (mapping dans `src/lib/categoriesStructurelles.ts` : une espèce → un champ T11, une volaille → deux champs T12 moderne/traditionnel, une espèce halieutique → deux champs T17 alevins/poisson) et apparaissent aussitôt dans la saisie DA et dans le rapport généré ; au rejet, l'item est simplement désactivé, aucun champ n'est créé. Le rapport reste **additif** : les colonnes canevas d'origine (`canevasLayout.ts`) ne sont jamais modifiées, les nouveaux champs approuvés sont uniquement ajoutés en fin de tableau (`fusionnerColonnes`/`fusionnerSousLignes` dans `buildReportTemplates.ts`), pour ne jamais trahir l'exigence « organisation identique au canevas papier ». Vérifié de bout en bout dans le navigateur : proposition d'une espèce par `admin.tech` → badge « en attente » → validation par `dd.menoua` (message listant le champ créé, `T11_CHEPTEL_YACK`) → la nouvelle ligne apparaît immédiatement sur `/da/saisie/T11` ; et circuit de rejet testé séparément (item désactivé, aucun `FormField` créé, confirmé en base).

## TODOs restants

- **Établissements de démonstration encore en base** : le module de gestion existe maintenant (`/etablissements`), mais les 2 établissements par arrondissement et par type créés par le seed sont toujours des données factices (« … (DÉMO) ») — à renommer ou désactiver un par un via le nouveau module une fois le vrai registre du DD disponible.
- **Reprise historique** : pas de module d'import Excel des mois antérieurs (M-1/N-1 seront vides tant qu'un mois réel n'a pas été bouclé).
- **Validation du dictionnaire** : la colonne « Validation DD (OUI/NON) » du classeur est vide pour toutes les lignes — le contenu (codes, libellés, unités, règle STOCK/FLUX) reste à valider formellement par le DD avant mise en production.
- **Polish visuel du .docx** : la génération programmatique (`buildReportTemplates.ts`) reproduit fidèlement le contenu (sections, libellés, totaux, en-tête bilingue) mais pas la mise en forme pixel-près du `.doc` original (logos image, bordures, polices exactes) — `templates/_reference_CANEVAS_STAT_MENOUA_officiel.docx` reste disponible comme référence si un habillage plus soigné est souhaité (peut être fait directement dans Word sur les templates générés, les balises `{...}` restant actives).
- **Comptes existants créés avant ce jalon** (issus du seed initial) : leur `mustChangePassword` reste `true` et ils seront donc redirigés vers la complétion de profil à leur prochaine connexion — comportement correct mais à anticiper en démonstration.
